{
  "name": "ui-ux-bugfix-demo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "start:dev": "node server.js",
    "test": "npx playwright test --config=playwright.config.js",
    "test:buggy": "npx playwright test tests/buggy.spec.ts --config=playwright.config.js",
    "test:fixed": "npx playwright test tests/fixed.spec.ts --config=playwright.config.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0"
  }
}
